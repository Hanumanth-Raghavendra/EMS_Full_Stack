import { useEffect,useState } from 'react';import{useNavigate,useParams}from'react-router-dom';import api from'../api/axios'
export default function EditUser(){const{userId}=useParams(),nav=useNavigate(),[emps,setEmps]=useState([]),[roles,setRoles]=useState([]),[form,setForm]=useState(null),[error,setError]=useState(''),[saving,setSaving]=useState(false);useEffect(()=>{Promise.all([api.get(`/api/users/${userId}`),api.get('/api/employees'),api.get('/api/roles')]).then(([u,e,r])=>{const x=u.data;setForm({employeeId:x.employeeId,username:x.username||'',passwordHash:'',roleId:x.roleId,enabled:Boolean(x.enabled)});setEmps(e.data);setRoles(r.data)}).catch(e=>setError(e.response?.data?.message||'Unable to load user.'))},[userId]);if(!form)return <div className="page-container"><p className="loading">{error||'Loading...'}</p></div>;const submit=async e=>{e.preventDefault();if(form.username.trim().length<3)return setError('Username must contain at least 3 characters.');try{setSaving(true);await api.put(`/api/users/${userId}`,{employeeId:Number(form.employeeId),username:form.username.trim(),passwordHash:form.passwordHash,roleId:Number(form.roleId),enabled:form.enabled});nav('/users')}catch(e){setError(e.response?.data?.message||'Unable to update user.')}finally{setSaving(false)}};return <div className="page-container"><div className="page-header"><div><h1>Edit User</h1><p>Update user #{userId}.</p></div><button className="secondary-button" onClick={()=>nav('/users')}>Back</button></div>{error&&<div className="error-banner">{error}</div>}<div className="form-card"><form onSubmit={submit}><div className="form-grid"><div className="form-group"><label>Employee</label><select value={form.employeeId} onChange={e=>setForm({...form,employeeId:e.target.value})}>{emps.map(x=><option key={x.employeeId} value={x.employeeId}>{x.employeeCode} — {x.firstName} {x.lastName}</option>)}</select></div><div className="form-group"><label>Username</label><input value={form.username} onChange={e=>setForm({...form,username:e.target.value})}/></div><div className="form-group"><label>New Password <span className="hint">(leave blank to keep current password)</span></label><input type="password" value={form.passwordHash} onChange={e=>setForm({...form,passwordHash:e.target.value})}/></div><div className="form-group"><label>Role</label><select value={form.roleId} onChange={e=>setForm({...form,roleId:e.target.value})}>{roles.map(x=><option key={x.roleId} value={x.roleId}>{x.roleName}</option>)}</select></div><label className="checkbox-row"><input type="checkbox" checked={form.enabled} onChange={e=>setForm({...form,enabled:e.target.checked})}/> Enabled</label></div><div className="form-actions"><button type="button" className="secondary-button" onClick={()=>nav('/users')}>Cancel</button><button className="primary-button" disabled={saving}>{saving?'Saving...':'Save Changes'}</button></div></form></div></div>}
